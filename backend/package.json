{
  "name": "unischeduler-backend",
  "version": "1.0.0",
  "description": "Backend API REST para el Sistema de Generación Óptima de Horarios Académicos",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seed/seed.js",
    "test": "jest --coverage",
    "test-mongo": "node -e \"const mongoose = require('mongoose'); const uri = process.env.MONGODB_URI || 'mongodb://localhost:27017/unischeduler'; mongoose.connect(uri, {serverSelectionTimeoutMS:5000}).then(()=>{console.log('✅ CONECTADO');return mongoose.connection.close();}).then(()=>{console.log('✅ Todo ok');process.exit(0);}).catch(e=>{console.log('❌ '+e.message);process.exit(1);})\""
  },
  "dependencies": {
    "@tgwf/co2": "^0.18.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^8.5.2",
    "express-validator": "^7.2.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb-memory-server": "^11.1.0",
    "mongoose": "^8.7.0",
    "morgan": "^1.10.0",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "node-mocks-http": "^1.17.2",
    "nodemon": "^3.1.7",
    "supertest": "^7.2.2"
  }
}
